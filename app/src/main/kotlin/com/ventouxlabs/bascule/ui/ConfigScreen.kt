package com.ventouxlabs.bascule.ui

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable

/**
 * Base URL, unit, contract version, always-on toggle, and the token — which is
 * never rendered back after saving; the field shows "set" or "not set"
 * (00-design.md §5).
 *
 * PHASE 2 PLACEHOLDER. Implemented in Phase 3 WP-28.
 */
@Composable
fun ConfigScreen() {
    Text("Settings — WP-28")
}
