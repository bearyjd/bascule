package com.ventouxlabs.bascule.ui

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable

/**
 * PWA-parity quick entry. Inserts source = MANUAL, which never dedups against a
 * scale reading (00-design.md §3.3).
 *
 * PHASE 2 PLACEHOLDER. Implemented in Phase 3 WP-24.
 */
@Composable
fun ManualEntryScreen() {
    Text("Manual entry — WP-24")
}
